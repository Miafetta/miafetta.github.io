# PR #55 依赖更新与文章渲染验证

验证日期：2026-09-20。环境：Windows、Node.js 24.21.0、pnpm 10.34.5。

## 结论与范围

PR 原始提交为 `25481a0`，依赖安装使用其锁文件。本次验证同时包含文章提交 `9ccfd4c`（11 篇更新、1 篇新增 Dockcheck 文章及封面），文章已独立上传 main，PR 同步 main 后保留依赖更新及本报告。

在下列检查范围内未发现依赖更新引入的回归，无需额外修改站点代码。既有本地文章改动全部保留。本次是依赖升级回归验证，不是重新统一全部文章代码块格式。

## 执行结果

| 检查 | 结果 |
| --- | --- |
| `pnpm install --frozen-lockfile` | 通过，未修改锁文件 |
| `pnpm type-check` | 通过 |
| `pnpm check` | 59 个文件，0 errors / 0 warnings / 0 hints |
| `pnpm build` | 通过，生成 17 个页面；Pagefind 索引 14 个页面 |
| `pnpm biome ci ./src --reporter=summary` | 69 个文件通过，未执行修复 |
| `git diff --check` | 通过 |
| 新生成 HTML 的步骤标签检查 | 12 篇文章、13 个标签、0 失败；检查器自测通过 |
| Markdown / RSS 补充样例 | 表格、列表、粗体、链接、代码转义、代码围栏、行内及块级数学公式通过 |
| RSS / sitemap XML 解析 | 通过；RSS 包含 12 篇文章 |

构建有一条非阻塞提示：astro-icon 找不到可选的 `src/icons` 目录。图标集正常构建，页面图标显示正常。Pagefind 提示中文不支持词干化，索引正常生成。

## Expressive Code

Expressive Code core、Astro 集成、行号和折叠插件均升级至 0.44.2；KaTeX 为 0.18.7，markdown-it 为 15.0.2。继续使用项目现有默认换行、语言对应行号设置、语言徽标及自定义复制按钮。

逐篇使用 Markdown 解析器枚举围栏，并核对构建产物。379 个围栏对应 379 个 Expressive Code 容器及 379 个复制按钮；渲染产物包含 1,130 个行号、54 个插入标记、4 个删除标记和 13 个步骤标签。

| 文章 | 代码块数 |
| --- | ---: |
| Docker 与 Docker Compose：入门与实践 | 77 |
| Jetson Nano 配置与 YOLO11 部署指南 | 13 |
| Linux 桌面应用常见问题修复 | 10 |
| RabbitMQ：消息队列入门与实践 | 48 |
| Scoop：Windows 软件包管理器入门 | 48 |
| Waydroid：在 Linux 上使用 Android 应用 | 42 |
| 使用 Dockcheck 自动更新容器 | 19 |
| 使用 Oh My Posh 美化 Windows 命令行 | 48 |
| 使用 Plymouth 美化 Linux 启动动画 | 13 |
| 在 Linux 下配置 Surface Laptop Go | 20 |
| 在树莓派上部署 Docker 应用 | 33 |
| 树莓派 USB-C 一线通配置指南 | 8 |
| **合计** | **379** |

就本次渲染回归检查而言：修改 0、未修改 379、待确认 0；实际编辑代码块 0。该统计不将会话开始前的用户文章修改计为本次修复，也不代表对每个代码示例的业务正确性作出保证。

步骤标签通过技能提供的只读检查器验证：每个 `.tm-label` 行中的 `.code` 都只有普通空白，不含被遮挡的命令、技术注释或不可见占位字符。Oh My Posh 文章保留本地修复后的 `2-3`、`5-6` 范围及两条原始 `reg import` 命令。

## 浏览器验证

使用 `pnpm preview --host 127.0.0.1 --port 4322` 检查本次生产构建：

- Oh My Posh 文章在 1440×1000 和 390×844 下检查截图，步骤标签各占空行，长命令在窄屏换行且没有覆盖；页面宽度与视口宽度一致，无整页横向溢出。
- 点击步骤代码块的复制按钮，从浏览器剪贴板核对两条完整命令及原始换行，未混入步骤标签或行号。
- Dockcheck 新文章在窄屏下核对配置表格、标题编号和代码块；复制配置命令后核对两行内容及换行。
- 检查文章中的文件标题、终端外观、行号、增删高亮及提示框均正常生成。

浏览器视觉检查为代表性页面抽查；全量代码块计数与标签空行检查覆盖全部文章。现有文章没有折叠代码块或数学公式示例，因此数学公式用独立 Markdown 渲染样例补充验证；折叠插件完成安装及构建验证，未声称完成折叠交互验收。

## 保留事项

文章中的省略号、路径占位符、示例输出、ANSI 控制符和业务代码均保留原样，没有执行文章中的命令。当前 PR 只更新依赖与记录验证结果；文章发布独立于 PR 合并。
